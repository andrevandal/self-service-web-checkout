services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000"
    environment:
      PORT: "3000"
      DATABASE_URL: "${DATABASE_URL}"
      DATABASE_AUTH_TOKEN: "${DATABASE_AUTH_TOKEN}"
