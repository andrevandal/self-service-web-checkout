services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      PORT: "3000"
      DATABASE_URL: file:/app/data/local.db
    volumes:
      - local-data:/app/data

volumes:
  local-data:
